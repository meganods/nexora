version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nexora_backend_api
    restart: always
    ports:
      - '5000:5000'
    env_file:
      - .env
    environment:
      - PORT=5000
      - NODE_ENV=production
